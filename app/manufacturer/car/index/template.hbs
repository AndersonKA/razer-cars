<div class="section section-group">

  <div class="columns">
    <div class="column is-half is-offset-one-quarter is-mobile">
      <nav class="panel">
        <p class="panel-heading title">
          {{model.name}}
        </p>
        <div class="panel-block">
          {{#link-to "manufacturer.car.new" class="button is-fullwidth is-info is-outlined" }}Add New Car{{/link-to}}
        </div>
      </nav>
    </div>
  </div>


  <div class="grid">
    {{#each model.cars as |car|}}
      <div class="grid__item">
        <nav class="panel">
          <p class="panel-heading title">
            {{car.year}} - {{car.name}}
          </p>
          <div class="panel-block">
            <p>Horsepower: {{car.hp}}</p>
            <p>Price: ${{car.price}}</p>
          </div>
        </nav>

      </div>
    {{/each}}
  </div>


</div>
